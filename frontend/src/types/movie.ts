export type Movie = {
  //I had to change the names to camel case for it to work
  movieId: number;
  category: string;
  title: string;
  year: number;
  director: string;
  rating: string;
  edited: string;
  lentTo: string;
  notes: string;
};
